<Window x:Class="UI.Vault_window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Vault_window" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Grid.Row="0" Content=" Account " Click="ButtonAccount_Click" Margin="3"/>
        <Button Grid.Column="1" Grid.Row="0" Content=" Settings " Click="ButtonSettings_Click" Margin="3"/>
        <Button Grid.Column="3" Grid.Row="0" Content="  QC  " Click="ButtonQC_Click" Margin="3"/>
        <DataGrid Grid.Row="1" Name="DataGridHistory" Margin="5" SelectionMode="Single" IsReadOnly="True" SelectionChanged="DataGridHRMedewerkers_SelectionChanged" Grid.ColumnSpan="4">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Open"/>
                    <MenuItem Header="Copy"/>
                    <MenuItem Header="Edit"/>
                    <MenuItem Header="Delete"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>
</Window>
